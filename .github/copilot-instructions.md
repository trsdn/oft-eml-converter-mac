# Copilot instructions

The authoritative instructions for this repository are in [`AGENTS.md`](../AGENTS.md). Read that file and follow it.

This file exists only so Copilot finds the instructions at the location it looks in. It deliberately does not restate them: two copies of the same rules drift apart, and an agent then follows whichever copy it happened to read.
