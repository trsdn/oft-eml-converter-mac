# Self-assessment

Per-criterion notes behind [`.github/conformance.yml`](../.github/conformance.yml).

- Standard: [Repository Quality Standard v1.5.1](https://github.com/trsdn/.github/blob/v1.5.1/docs/repository-quality-standard.md)
- Assessed on: 2026-08-30
- State: **Needs work**

The record is the machine-readable result; this document holds the reasoning. Where a criterion is `na`, the rationale is here rather than implied.

## Profiles claimed

| Profile | Applies | Why |
|---|---|---|
| Baseline | yes | Active repository |
| Public | yes | Publicly visible |
| Software | yes | Builds and executes application code |
| Deployable | **no** | Installed by the user on their own Mac. Nothing is deployed to a server, container, or cloud environment |
| Package | yes | A `v*` tag produces a signed, notarized DMG published as a release asset |
| Documentation | **no** | The product is an application; `docs/` supports it |
| Published Site | yes | GitHub Pages at the repository homepage, aimed at people who want the app and not the source |
| Product Identity | yes | Ships a signed application bundle |
| Agent Readiness | yes | Required in full by the Software and Package profiles |
| Language | yes | |
| Accessibility | yes | Ships a graphical user interface and a site |
| Data Protection | yes | Processes user files |
| Archived | **no** | Actively maintained |

## Baseline

| ID | Result | Note |
|---|---|---|
| B01 | pass | Name and description state the purpose |
| B02 | pass | README covers purpose, audience, status, setup, and outward links |
| B03 | pass | MIT `LICENSE`, with the `extract_msg` GPL-3.0 boundary stated |
| B04 | pass | Build output, `venv/`, and `.release.env` ignored; all maintained source tracked |
| B05 | pass | `./scripts/test.sh`, documented in README and `AGENTS.md` |
| B06 | pass | Ruleset on `main` requiring `Test Suite`; no unresolved critical alerts |
| B07 | pass | macOS 14 minimum set by the build and asserted against the README badge |
| B08 | pass | `CHANGELOG.md` and GitHub releases |
| B09 | pass | Public, topics set, homepage points at the site, not archived |
| B10 | pass | Maintainer and best-effort support status stated in the README |
| B11 | pass | This assessment and the record it backs |
| B12 | pass | `trsdn-standard` topic present |
| B13 | pass | The README carried a second copy of the repository layout; it now links to `AGENTS.md`, which owns it |

## Public

| ID | Result | Note |
|---|---|---|
| P01 | pass | MIT, recognized by GitHub |
| P02 | pass | `CONTRIBUTING.md` in-repo; conduct policy inherited from `trsdn/.github` |
| P03 | pass | `SECURITY.md` added, private vulnerability reporting already enabled |
| P04 | pass | Issue forms in-repo, pull-request template inherited from `trsdn/.github` |
| P05 | pass | Install, usage, compatibility, data handling, security, and support status all covered |
| P06 | pass | Community Standards reports 100% |
| P07 | pass | Description, ten topics, and a maintained homepage |
| P08 | pass | Required order restored, CI and release badges added, and the hand-written macOS badge is covered by a drift check against `LSMinimumSystemVersion` |
| P09 | pass | Self-hosted card generated by `.github/workflows/stats.yml` |

## Software

| ID | Result | Note |
|---|---|---|
| S01 | **partial** | Setup is documented and reproducible, but `extract_msg` is installed unpinned, so two clean installs can resolve different versions |
| S02 | pass | Python converter tests and Swift integration tests, run in CI |
| S03 | **fail** | No formatter, linter, or static analysis runs. Both `swift-format` and a Python linter are available and neither is wired up |
| S04 | pass | macOS is the only supported platform and CI covers it |
| S05 | pass | Secret scanning and push protection enabled |
| S06 | na | No environment-driven configuration surface. `.release.env` is build-time signing configuration, covered by `D02`-style handling and excluded from the repository |
| S07 | pass | Errors name the file and the failing step. Nothing is transmitted, so nothing can leak outward |
| S08 | pass | `.github/dependabot.yml` with alerts and security updates enabled |
| S09 | pass | Ruleset requires `Test Suite` and blocks force pushes and deletion |
| S10 | pass | `docs/ARCHITECTURE.md` and `docs/TESTING.md` |

## Package And Release

| ID | Result | Note |
|---|---|---|
| R01 | pass | `Info.plist` carries name, version, identifier, copyright, licence, and both URLs, all agreeing with repository metadata |
| R02 | pass | Semantic versioning and the tag-to-bundle relationship documented in the README |
| R03 | pass | A `v*` tag produces a signed, notarized DMG with a checksum, uploaded to the release |
| R04 | pass | `scripts/build-release.sh` refuses to continue when the requested version and the built bundle disagree, and CI asserts the build honours `APP_VERSION` |
| R05 | **partial** | The DMG is verified with `hdiutil verify` and `codesign --verify`, which proves it is intact and correctly signed. The application inside it is never launched in a clean environment |
| R06 | pass | Release notes plus a changelog that calls out the bundle identifier change as an upgrade concern |

## Product Identity

| ID | Result | Note |
|---|---|---|
| I01 | pass | Name and version embedded, version taken from the tag |
| I02 | pass | `TRSDNRepositoryURL` and `TRSDNIssueTrackerURL` in `Info.plist` |
| I03 | pass | `NSHumanReadableCopyright` and `TRSDNLicense`; the DMG carries the licence text |
| I04 | pass | About panel shows version and build and links to the repository and issue tracker; Help menu offers "Report an Issue" |
| I05 | **partial** | The icon is embedded in the bundle and appears in the installer, but the published site shows a drawn mockup rather than the product icon, so the surfaces are not consistent |
| I06 | pass | Every identity value is written by `scripts/build.sh` from the tag and the repository. Two CI checks fail if a literal creeps back in |

## Published Site

| ID | Result | Note |
|---|---|---|
| W01 | pass | GitHub Pages from `docs/` on `main`, documented in the README |
| W02 | pass | Repository homepage points at the site; the site links back in the navigation and the footer |
| W03 | pass | Name, purpose, audience, maintenance status, and the version described are all above the fold |
| W04 | pass | Content baseline complete: status and version, what it does, how to get it, the `Y01` disclosure, links to repository, licence, security policy and support, and a review date |
| W05 | **fail** | The page uses hand-written CSS rather than the shared Instrument Workshop design language. Adopting it is a redesign, not an edit |
| W06 | **fail** | No design language version is recorded, because none is adopted. `W06` cannot pass while `W05` fails |
| W07 | pass | No external fonts, scripts, images, or analytics. A CI check now fails if a third-party sub-resource is added |
| W08 | pass | Contributor-facing material stays in the repository; the site links out for depth |

## Agent Readiness

| ID | Result | Note |
|---|---|---|
| G01 | pass | `AGENTS.md` at the root. It was `AGENT.md`, which agents do not look for |
| G02 | pass | Purpose, layout, setup, run, and validation commands, each verified |
| G03 | pass | History rewriting, secrets, `.release.env`, signing, notarization, tags, releases, the bundle identifier, and network access all named |
| G04 | pass | `.github/copilot-instructions.md` and `.github/github-app.yml` reference `AGENTS.md` rather than restating it |
| G05 | pass | `./scripts/test.sh` succeeds from a clean checkout |
| G06 | pass | Generated paths listed in `AGENTS.md`, including the fact that `Info.plist` is rewritten on every build |
| G07 | pass | `Co-authored-by:` trailer plus a stated pull-request and review expectation, and an explicit rule that automation does not publish releases |
| G08 | pass | `.github/github-app.yml` present and intentional |

## Language And Localization

| ID | Result | Note |
|---|---|---|
| L01 | pass | English declared in the README |
| L02 | pass | All interface strings are English |
| L03 | pass | English-only declared |
| L04 | na | No string catalogs |
| L05 | na | No locale-sensitive formatting. Message dates pass through as the source file recorded them |
| L06 | na | No translations |
| L07 | pass | Repository, docs, comments, identifiers, commits, and issues are English |

## Accessibility

| ID | Result | Note |
|---|---|---|
| X01 | **unknown** | The window can be operated with the file picker rather than by dragging, but full keyboard operability and focus order have not been verified |
| X02 | **unknown** | The custom drop area's accessible name and role come from SwiftUI defaults and have not been inspected |
| X03 | **unknown** | The app inherits system text sizing and Increase Contrast by being standard SwiftUI, but contrast has not been measured. The site now honours `prefers-reduced-motion` |
| X04 | pass | `src/converter.py` prints plain ASCII without colour or Unicode decoration |
| X05 | pass | The README states which limitations are unverified and points at the tracking issue |

## Data Protection And Privacy

| ID | Result | Note |
|---|---|---|
| Y01 | pass | README, site, and `SECURITY.md` all state that nothing is collected or transmitted |
| Y02 | pass | Both destinations named: PyPI once on first launch, and Gatekeeper's notarization check performed by macOS |
| Y03 | na | No telemetry, analytics, or crash reporting exists to disable |
| Y04 | pass | `~/Library/Application Support/OFT-EML-Converter/` named, with removal explained |
| Y05 | na | No third-party service or AI provider receives user content |
| Y06 | na | Nothing outlives a session except the output files the user asked for |

## Deployable, Documentation, Archived

`D01`–`D06`, `T01`–`T05`, and `A01`–`A04` are `na`. The profile rationale is in the table above.

## Outstanding work

1. `S03` — wire up `swift-format` and a Python linter in CI.
2. `W05` and `W06` — adopt the Instrument Workshop design language and record the version vendored. Tracked in [#12](https://github.com/trsdn/oft-eml-converter-mac/issues/12).
3. `R05` — launch the built app in a clean runner and assert it starts, rather than only verifying the DMG.
4. `I05` — use the product icon on the site.
5. `S01` — pin `extract_msg` or accept resolver drift deliberately.
6. `X01`–`X03` — verify against VoiceOver and keyboard-only operation. Tracked in [#10](https://github.com/trsdn/oft-eml-converter-mac/issues/10).
